'use client';
import { useMemo, useState } from 'react';
import { Activity, CheckCircle2, FileUp, FolderKanban, ListTodo, Search } from 'lucide-react';
import { MOCK_ACTIVITY_LOG } from '@/data/mock-workspace';
import { PageIntro, Pill, WorkspaceCard } from '@/components/workspace/WorkspacePrimitives';
const icons = { TASK_CREATED: ListTodo, TASK_COMPLETED: CheckCircle2, FILE_UPLOADED: FileUp, PROJECT_UPDATED: FolderKanban, APPROVAL_APPROVED: CheckCircle2 } as const;
export default function ActivityPage() { const [query, setQuery] = useState(''); const [team, setTeam] = useState('ALL'); const teams = ['ALL', ...Array.from(new Set(MOCK_ACTIVITY_LOG.map((item) => item.actorTeam)))]; const entries = useMemo(() => MOCK_ACTIVITY_LOG.filter((item) => `${item.actorName} ${item.actorTeam} ${item.description} ${item.targetName ?? ''}`.toLowerCase().includes(query.toLowerCase()) && (team === 'ALL' || item.actorTeam === team)), [query, team]); return <div className="space-y-6"><PageIntro eyebrow="Audit trail" title="Organization activity" description="A transparent timeline of task, file, project, event, approval, and member changes across the committee." /><div className="flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search activity..." className="w-full rounded-xl border border-border/70 bg-card/60 py-3 pl-9 pr-3 text-sm outline-none focus:border-cyan-400/50" /></div><select value={team} onChange={(e) => setTeam(e.target.value)} className="rounded-xl border border-border/70 bg-card/60 px-3 py-3 text-sm outline-none"><option>ALL</option>{teams.slice(1).map((item) => <option key={item}>{item}</option>)}</select></div><WorkspaceCard><div className="relative space-y-6 before:absolute before:bottom-2 before:left-5 before:top-2 before:w-px before:bg-border">{entries.map((item) => { const Icon = icons[item.type as keyof typeof icons] ?? Activity; return <div key={item.id} className="relative flex gap-4"><div className="z-10 flex h-10 w-10 shrink-0 items-center justify-center rounded-xl border border-cyan-400/25 bg-card text-cyan-300"><Icon className="h-4 w-4" /></div><div className="min-w-0 flex-1 rounded-xl border border-border/60 bg-muted/20 p-3"><div className="flex flex-wrap items-center justify-between gap-2"><p className="text-sm"><span className="font-semibold">{item.actorName}</span> <span className="text-muted-foreground">{item.description}</span></p><span className="text-[10px] text-muted-foreground">{item.timestamp}</span></div>{item.targetName && <p className="mt-2 text-xs font-medium text-cyan-300">{item.targetName}</p>}<div className="mt-2"><Pill>{item.actorTeam}</Pill></div></div></div>})}</div></WorkspaceCard></div>; }
