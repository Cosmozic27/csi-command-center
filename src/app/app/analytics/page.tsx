'use client';
import { BarChart3, CheckCircle2, Clock3, ShieldAlert, TrendingUp } from 'lucide-react';
import { ANALYTICS } from '@/data/mock-workspace';
import { PageIntro, ProgressBar, SectionTitle, WorkspaceCard } from '@/components/workspace/WorkspacePrimitives';
const metrics = [{ label: 'Completion rate', value: '68%', delta: '+12%', icon: CheckCircle2, color: 'text-emerald-400' }, { label: 'Open workload', value: '31', delta: '-8%', icon: TrendingUp, color: 'text-cyan-400' }, { label: 'Approval turnaround', value: '1.8d', delta: '-0.4d', icon: Clock3, color: 'text-violet-400' }, { label: 'Blocked items', value: '4', delta: '+1', icon: ShieldAlert, color: 'text-red-400' }];
export default function AnalyticsPage() { return <div className="space-y-6"><PageIntro eyebrow="Operational intelligence" title="Committee analytics" description="Useful signals for capacity planning, delivery health, and approval throughput—not decorative charts." /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({ label, value, delta, icon: Icon, color }) => <WorkspaceCard key={label}><Icon className={`h-5 w-5 ${color}`} /><p className="mt-4 text-xs text-muted-foreground">{label}</p><div className="mt-1 flex items-end justify-between"><p className="text-2xl font-bold">{value}</p><span className={`text-xs ${delta.startsWith('+') && label === 'Blocked items' ? 'text-red-300' : 'text-emerald-300'}`}>{delta}</span></div></WorkspaceCard>)}</div><div className="grid gap-5 lg:grid-cols-2"><WorkspaceCard><SectionTitle title="Task health" detail="Current distribution across all teams" /><div className="space-y-4">{ANALYTICS.taskHealth.map((item) => <div key={item.label}><div className="mb-1 flex justify-between text-xs"><span className="text-muted-foreground">{item.label}</span><span className="font-mono text-foreground">{item.value}</span></div><ProgressBar value={(item.value / 73) * 100} color={item.color} /></div>)}</div></WorkspaceCard><WorkspaceCard><SectionTitle title="Team workload" detail="Open work relative to healthy operating capacity" /><div className="space-y-4">{ANALYTICS.teamLoad.map((item) => <div key={item.label}><div className="mb-1 flex justify-between text-xs"><span className="text-muted-foreground">{item.label}</span><span className="font-mono text-foreground">{item.value}%</span></div><ProgressBar value={item.value} color={item.value > 75 ? 'bg-amber-400' : 'bg-cyan-400'} /></div>)}</div></WorkspaceCard></div><WorkspaceCard><SectionTitle title="Delivery narrative" detail="A concise operational readout for Faculty and Core" /><div className="grid gap-3 md:grid-cols-3"><div className="rounded-xl border border-emerald-400/20 bg-emerald-400/5 p-4"><BarChart3 className="h-4 w-4 text-emerald-400" /><p className="mt-3 text-sm font-semibold">Momentum is healthy</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Completion is up while the open workload is trending down.</p></div><div className="rounded-xl border border-amber-400/20 bg-amber-400/5 p-4"><Clock3 className="h-4 w-4 text-amber-400" /><p className="mt-3 text-sm font-semibold">Technical is at capacity</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Prioritize handoffs before adding more launch-critical work.</p></div><div className="rounded-xl border border-violet-400/20 bg-violet-400/5 p-4"><CheckCircle2 className="h-4 w-4 text-violet-400" /><p className="mt-3 text-sm font-semibold">Approvals are moving</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Average review turnaround is below the two-day operating target.</p></div></div></WorkspaceCard></div>; }
