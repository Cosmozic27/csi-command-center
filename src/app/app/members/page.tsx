'use client';
import { useMemo, useState } from 'react';
import { Search, Users } from 'lucide-react';
import { MOCK_MEMBERS } from '@/data/mock-workspace';
import { PageIntro, Pill, ProgressBar, SectionTitle, WorkspaceCard } from '@/components/workspace/WorkspacePrimitives';
export default function MembersPage() { const [query, setQuery] = useState(''); const [team, setTeam] = useState('ALL'); const [selected, setSelected] = useState(MOCK_MEMBERS[0]); const teams = ['ALL', ...Array.from(new Set(MOCK_MEMBERS.map((m) => m.team)))]; const visible = useMemo(() => MOCK_MEMBERS.filter((m) => `${m.fullName} ${m.team} ${m.skills.join(' ')}`.toLowerCase().includes(query.toLowerCase()) && (team === 'ALL' || m.team === team)), [query, team]); return <div className="space-y-6"><PageIntro eyebrow="People & capacity" title="Member directory" description="Find the right people, see skills and workload, and keep collaboration focused on committee work." /><div className="flex flex-col gap-3 lg:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search members or skills..." className="w-full rounded-xl border border-border/70 bg-card/60 py-3 pl-9 pr-3 text-sm outline-none focus:border-cyan-400/50" /></div><select value={team} onChange={(e) => setTeam(e.target.value)} className="rounded-xl border border-border/70 bg-card/60 px-3 py-3 text-sm text-foreground outline-none">{teams.map((item) => <option key={item}>{item}</option>)}</select></div><div className="grid gap-5 xl:grid-cols-[1fr_0.8fr]"><div className="grid gap-3 md:grid-cols-2">{visible.map((member) => <button key={member.id} onClick={() => setSelected(member)} className="text-left"><WorkspaceCard className="h-full hover:border-cyan-400/40"><div className="flex items-start gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-cyan-400/10 font-bold text-cyan-300">{member.initials}</div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><h3 className="font-semibold">{member.fullName}</h3><Pill tone={member.status === 'ACTIVE' ? 'emerald' : 'amber'}>{member.status}</Pill></div><p className="mt-1 text-xs text-muted-foreground">{member.primaryRole.replace('_', ' ')} · {member.team}</p></div></div><div className="mt-4 flex flex-wrap gap-1.5">{member.skills.map((skill) => <Pill key={skill}>{skill}</Pill>)}</div><div className="mt-4 flex items-center justify-between text-xs"><span className="text-muted-foreground">{member.workload} active assignments</span><span className="text-cyan-300">View profile →</span></div></WorkspaceCard></button>)}</div><WorkspaceCard><SectionTitle title="Member profile" detail="Operational context only; no unnecessary personal information" /><div className="flex items-center gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-violet-400/10 font-bold text-violet-300">{selected.initials}</div><div><h2 className="font-bold">{selected.fullName}</h2><p className="text-xs text-muted-foreground">{selected.team} · {selected.primaryRole.replace('_', ' ')}</p></div></div><div className="mt-6"><p className="text-xs text-muted-foreground">Current workload</p><div className="mt-2 flex items-center gap-3"><ProgressBar value={Math.min(100, selected.workload * 8)} color="bg-violet-400" /><span className="font-mono text-xs">{selected.workload}/12</span></div></div><div className="mt-6"><p className="text-xs text-muted-foreground">Skills</p><div className="mt-2 flex flex-wrap gap-2">{selected.skills.map((skill) => <Pill key={skill} tone="violet">{skill}</Pill>)}</div></div><div className="mt-6 rounded-xl border border-cyan-400/15 bg-cyan-400/5 p-4 text-xs leading-5 text-muted-foreground"><Users className="mb-2 h-4 w-4 text-cyan-400" />Assigned work and project participation are ready to map to the future member service.</div></WorkspaceCard></div></div>; }
